import styled from "@emotion/styled";
import {Container} from "@mantine/core";

export const PreGraduatedScreenContainer = styled(Container)<{container_width}>`
  min-width: 100%;
  padding: 0;
  display: flex;
  justify-content: center;
  align-items: flex-start;
`;
