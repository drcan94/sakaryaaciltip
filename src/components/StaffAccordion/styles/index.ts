import styled from "@emotion/styled";
import {Container} from "@mantine/core";

export const StaffContainer = styled(Container)`
  display: flex;
  flex-wrap: wrap;
  justify-content: space-evenly;
  align-items: stretch;
  padding-top: 20px;
  min-width: 100%;
`;

